# [ coa brain unit ]
# family: fedora
# area:   boot
# --------------------------------------------------

boot:
  params: "root=live:CDLABEL=OA_LIVE rd.live.image rd.live.dir=live rd.live.squashimg=filesystem.squashfs selinux=0"
